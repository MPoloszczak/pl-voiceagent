# Production-ready requirements with exact versions to eliminate conflicts
# Optimized for fast installation and HIPAA compliance

# Core OpenAI and MCP dependencies
openai-agents==0.0.16
openai-agents-mcp==0.0.5

# Audio processing (exact versions)
deepgram-sdk==3.10.1
elevenlabs==1.5.0
pyaudio==0.2.14
webrtcvad==2.0.10

# Web framework (FastAPI stack - exact versions)
fastapi==0.112.0
uvicorn==0.34.0
websockets==12.0
python-multipart==0.0.12

# HTTP client (pinned version to avoid conflicts)
httpx==0.28.1
# Required by httpx[http2]
h2==4.1.0
httpcore==1.0.7

# Data handling and validation (exact versions)
pydantic==2.10.3
pydantic-core==2.27.1
python-dotenv==1.0.1
aiofiles==23.2.1

# Type compatibility (exact versions to avoid conflicts)
typing_extensions==4.12.2

# Security and authentication
PyJWT==2.9.0
certifi==2024.8.30

# Database and caching (exact versions)
redis==5.0.4
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.36

# AWS integration (exact versions)
boto3==1.35.78
aws-xray-sdk==2.14.0

# MCP server framework
fastmcp==2.3.3

# Core dependencies (pinned for stability)
idna==3.10
sniffio==1.3.1
anyio==4.6.2
h11==0.14.0